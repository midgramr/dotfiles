general {
   output_format = "i3bar"
   separator = "  "
   color_good = "#a7c080"
   color_degraded = "#dbbc7f"
   color_bad = "#e67e80"
}

order += "memory"
order += "disk /"
order += "volume master"
order += "time"

disk "/" {
   format = "Disk %avail/ %total"
}

memory {
    format = "Mem %used / %total"
}

volume master {
    format = "♪: %volume"
    format_muted = "♪: muted (%volume)"
    device = "default"
}

time {
    format = "%a %b %d %l:%M %p"
}

# vim: ft=conf
